- name: Install packages
  become: yes
  pacman:
    state: present
    name:
      - aspell
      - aspell-en
      - aspell-pt
      - dnsutils
      - docker
      - emacs
      - git-lfs
      - gnupg
      - go
      - gopass
      - kubectl
      - nano
      - tmux

- name: Install AUR packages
  aur:
    upgrade: no
    skip_installed: yes
    name:
      - cfssl
      - chef-dk
      - google-cloud-sdk
      - kail
      - pyenv-virtualenv
      - zoom
      - zsa-wally
